@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Clean Architecture Demo</h1>
    <p class="lead">Démo d'architecture Clean Architecture 3 couches avec ASP.NET Core 8</p>
</div>

<!-- Cards côte à côte sur grand écran, colonne sur mobile -->
<div class="row mt-5 g-4">
    <!-- WeatherForecast CRUD -->
    <div class="col-lg-6">
        <div class="card h-100">
            <div class="card-header bg-primary text-white">
                <h3 class="mb-0">WeatherForecast CRUD</h3>
            </div>
            <div class="card-body d-flex flex-column">
                <p>Cette application démontre l'architecture Clean Architecture avec :</p>
                <ul class="flex-grow-1">
                    <li><strong>Domain</strong> : Entités et Interfaces (WeatherForecast, IUnitOfWork)</li>
                    <li><strong>Infrastructure</strong> : Repositories et DbContext (EF Core InMemory)</li>
                    <li><strong>Web</strong> : Controllers et Views (Razor)</li>
                </ul>
                <div class="mt-auto">
                    <a asp-controller="WeatherForecast" asp-action="Index" class="btn btn-primary btn-lg w-100">
                        Voir les prévisions météo
                    </a>
                </div>
            </div>
        </div>
    </div>

    <!-- API REST -->
    <div class="col-lg-6">
        <div class="card api-card h-100">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h3 class="mb-0">🔌 API REST</h3>
                <span class="badge bg-success">OpenAPI / Swagger</span>
            </div>
            <div class="card-body d-flex flex-column">
                <p>API REST complète avec authentification JWT et documentation interactive :</p>
                <ul class="flex-grow-1">
                    <li><strong>Endpoints</strong> : WeatherForecast CRUD via HTTP</li>
                    <li><strong>Authentification</strong> : JWT Bearer Token</li>
                    <li><strong>Documentation</strong> : Swagger UI intégré</li>
                    <li><strong>Base de données</strong> : PostgreSQL partagée avec l'application Web</li>
                </ul>
                <div class="mt-auto">
                    <a href="https://localhost:7252/swagger/index.html" target="_blank" class="btn btn-success btn-lg w-100">
                        📚 Documentation Swagger
                    </a>
                    <div class="alert alert-info mt-3 mb-0">
                        <small>
                            <strong>💡 Astuce :</strong> L'API doit être démarrée
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4 g-4">
    <div class="col-md-4">
        <div class="card h-100">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">📦 Domain</h5>
                <p class="card-text flex-grow-1">Entités métier pures sans dépendances externes</p>
            </div>
        </div>
    </div>
    <div class="col-md-4">
        <div class="card h-100">
            <div class="card-body d-flex flex-column">
                <h5 class="card-title">🔧 Infrastructure</h5>
                <p class="card-text flex-grow-1">Implémentations techniques (EF Core, Repositories)</p>
            </div>
        </div>
    </div>
</div>
